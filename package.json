{
  "name": "oakandpixel-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'static site - no build required'",
    "validate": "node scripts/validate-site.mjs"
  }
}
